interface User {
    id: string;
    name: string;
    email: string;
    phone: string;
    password: string;
    isActive: boolean;
}

type Product = {
    id: string;
    name: string;
    price: number;
};
